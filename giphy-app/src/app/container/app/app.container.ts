import { Component } from '@angular/core';

@Component({
  selector: 'app',
  styleUrls: ['./app.container.less'],
  template: `
    <router-outlet></router-outlet>
  `
})
export class AppContainer {
}
